{
    "_id": "{{@ system @}}",
    "authentication": "basic",
    "operations": {
        "list-customer": {
            "method": "GET",
            "url": "customer"
        }
    },
    "password": "$SECRET(tripletex-password)",
    "type": "system:rest",
    "url_pattern": "{{@ base_url @}}/v2/%s",
    "username": "0",
    "verify_ssl": true
}
